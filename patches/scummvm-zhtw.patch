diff --git a/engines/scumm/charset.cpp b/engines/scumm/charset.cpp
index 190e1818..33e25abe 100644
--- a/engines/scumm/charset.cpp
+++ b/engines/scumm/charset.cpp
@@ -106,6 +106,9 @@ void ScummEngine::loadCJKFont() {
 		_2byteFontPtr[0] = 0xFF;
 	} else if (_language == Common::KO_KOR ||
 			   (_game.version >= 7 && (_language == Common::JA_JPN || _language == Common::ZH_TWN)) ||
+			   // 繁中化（Maniac Mansion V1/V2）：原本的 gate 是 version >= 3，把 SCUMM v1/v2
+			   // 整批擋在 ZH_CHN 路徑外面。
+			   (_game.version <= 2 && _game.id == GID_MANIAC && _language == Common::ZH_CHN) ||
 			   (_game.version >= 3 && _language == Common::ZH_CHN)) {
 		int numChar = 0;
 		const char *fontFile = nullptr;
@@ -130,7 +133,13 @@ void ScummEngine::loadCJKFont() {
 			numChar = 13630;
 			break;
 		case Common::ZH_CHN:
-			if (_game.id == GID_FT || _game.id == GID_LOOM || _game.id == GID_INDY3 ||
+			if (_game.id == GID_MANIAC && _game.version <= 2) {
+				// 繁中化自訂碼空間：首碼 0x88-0x9F、尾碼 0xA1-0xFD，共 24 * 93 = 2232 字。
+				// 首碼必須避開 SCUMM v2 空白壓縮（「位元組 | 0x80」= 後接一個空白）會產生的
+				// 值：可列印 ASCII | 0x80 = 0xA0-0xFE，控制碼 0x01-0x03 | 0x80 = 0x81-0x83。
+				fontFile = "chinese_gb16x12.fnt";
+				numChar = 2232;
+			} else if (_game.id == GID_FT || _game.id == GID_LOOM || _game.id == GID_INDY3 ||
 				_game.id == GID_INDY4 || _game.id == GID_MONKEY || _game.id == GID_MONKEY2 ||
 				_game.id == GID_TENTACLE) {
 				fontFile = "chinese_gb16x12.fnt";
@@ -163,6 +172,54 @@ void ScummEngine::loadCJKFont() {
 				_newLineCharacter = 0x21;
 				break;
 			case Common::ZH_CHN:
+				if (_game.id == GID_MANIAC && _game.version <= 2) {
+					// 繁中化：倚天中文系統 (ETEN 3.53) 的原生點陣字，兩種尺寸都支援，
+					// **由字型檔的大小決定**（numChar 固定 2232）：
+					//   2232 x 30 bytes → 16x15（倚天 15 點，邏輯 8x7.5，與原版 ASCII 同大）
+					//   2232 x 72 bytes → 24x24（倚天 24 點，邏輯 12x12，比原版大一半）
+					//
+					// 排版一律以**邏輯像素**計：文字表面放大 2 倍，所以字模寬高各除以 2
+					// 就是邏輯尺寸。換算由 printChar() 既有的
+					// `if (is2byte) { origWidth /= _textSurfaceMultiplier; ... }` 與
+					// getCharWidth() 的 `_2byteWidth / 2` 負責，與 FM-Towns 的 CJK 走法一致。
+					_2byteWidth = (fp.size() >= (int64)numChar * 72) ? 24 : 16;
+					_2byteHeight = (_2byteWidth == 24) ? 24 : 15;
+					_newLineCharacter = 0x21;
+					_textSurfaceMultiplier = 2;
+					break;
+				}
+				if (_game.id == GID_TENTACLE) {
+					// 繁中化（瘋狂時代）：字模尺寸同樣由字型檔的大小決定，numChar 固定 8178。
+					//   8178 x 24 bytes → 12x12（原版 ZH_CHN 規格）
+					//   8178 x 72 bytes → 24x24（倚天 24 點，畫在 2 倍文字表面上，邏輯 12x12）
+					// 24x24 走 hi-res：原始美術由 drawStripToScreen() 做 2x nearest 放大，
+					// 中文字直接畫進 2 倍的文字表面，所以同一畫面「美術照原樣、中文銳利」。
+					//   8178 x 28 bytes → 16x14（倚天 15 點裁掉全空的最後一列，邏輯 8x7）
+					//   8178 x 30 bytes → 16x15（倚天 15 點原尺寸，邏輯 8x7.5）
+					//
+					// 為什麼預設是 16x14 而不是 16x15：本作的指令區裡，句子列在 y=145、
+					// 指令第一列在 y=152，只差 7 個邏輯像素（三列指令之間反而寬到 16）。
+					// 邏輯高 7.5 會讓句子列的重畫範圍咬到第一列，把「使用」那格擦掉；
+					// 裁掉倚天 15 點那條全空的最後一列之後邏輯高剛好 7，與原版英文的
+					// 幾何一致，版面一處都不必改。
+					const int bytesPerGlyph = (int)(fp.size() / numChar);
+					if (bytesPerGlyph >= 72) {
+						_2byteWidth = _2byteHeight = 24;
+						_textSurfaceMultiplier = 2;
+					} else if (bytesPerGlyph >= 30) {
+						_2byteWidth = 16;
+						_2byteHeight = 15;
+						_textSurfaceMultiplier = 2;
+					} else if (bytesPerGlyph >= 28) {
+						_2byteWidth = 16;
+						_2byteHeight = 14;
+						_textSurfaceMultiplier = 2;
+					} else {
+						_2byteWidth = _2byteHeight = 12;
+					}
+					_newLineCharacter = 0x21;
+					break;
+				}
 				_2byteWidth = 12;
 				_2byteHeight = 12;
 				_newLineCharacter = 0x21;
@@ -174,6 +231,53 @@ void ScummEngine::loadCJKFont() {
 			_2byteFontPtr = new byte[((_2byteWidth + 7) / 8) * _2byteHeight * numChar];
 			fp.read(_2byteFontPtr, ((_2byteWidth + 7) / 8) * _2byteHeight * numChar);
 			fp.close();
+
+			// 繁中化：指令列（給予／打開／查看…）另外用一套字型。遊戲夾裡放
+			// chinese_verb.fnt 就會啟用，格式與尺寸跟正文字型完全相同——只是字面
+			// 不一樣，所以版面、行高、hit-test 一概不受影響。沒放就整個關掉。
+			//
+			// 字模尺寸由檔案大小回推（這個格式沒有檔頭）：每字 bytes = 檔長 / numChar，
+			// 支援兩種——28 = 16x14（與正文同尺寸），72 = 24x24（指令列列距 16 邏輯
+			// 像素 = 32 實體，放得下）。其他大小一律忽略，不猜。
+			ScummFile verbFp(this);
+			if (openFile(verbFp, "chinese_verb.fnt")) {
+				const int32 per = verbFp.size() / numChar;
+				if (per == 28) {
+					_2byteVerbWidth = 16; _2byteVerbHeight = 14;
+				} else if (per == 72) {
+					_2byteVerbWidth = _2byteVerbHeight = 24;
+				} else {
+					warning("chinese_verb.fnt 每字 %d bytes，不是 28（16x14）或 72（24x24），忽略",
+							(int)per);
+					_2byteVerbWidth = 0;
+				}
+				if (_2byteVerbWidth) {
+					const uint32 sz = ((_2byteVerbWidth + 7) / 8) * _2byteVerbHeight * numChar;
+					_2byteVerbFontPtr = new byte[sz];
+					verbFp.read(_2byteVerbFontPtr, sz);
+				}
+				verbFp.close();
+			}
+
+			// 對白字型，載入規則與 verb 字型相同
+			ScummFile dlgFp(this);
+			if (openFile(dlgFp, "chinese_dialog.fnt")) {
+				const int32 per = dlgFp.size() / numChar;
+				if (per == 28) {
+					_2byteDialogWidth = 16; _2byteDialogHeight = 14;
+				} else if (per == 72) {
+					_2byteDialogWidth = _2byteDialogHeight = 24;
+				} else {
+					warning("chinese_dialog.fnt 每字 %d bytes，不是 28 或 72，忽略", (int)per);
+					_2byteDialogWidth = 0;
+				}
+				if (_2byteDialogWidth) {
+					const uint32 sz = ((_2byteDialogWidth + 7) / 8) * _2byteDialogHeight * numChar;
+					_2byteDialogFontPtr = new byte[sz];
+					dlgFp.read(_2byteDialogFontPtr, sz);
+				}
+				dlgFp.close();
+			}
 		} else {
 			if (fontFile)
 				error("SCUMM::Font: Could not open %s",fontFile);
@@ -310,11 +414,27 @@ byte *ScummEngine::get2byteCharPtr(int idx) {
 			return _2byteFontPtr + base;
 		}
 	case Common::ZH_CHN:
+		if (_game.id == GID_MANIAC && _game.version <= 2) {
+			// 自訂碼空間：首碼 0x88-0x9F、尾碼 0xA1-0xFD（見 loadCJKFont 的說明）
+			int lead = idx % 256;
+			int trail = idx / 256;
+			if (lead < 0x88 || lead > 0x9F || trail < 0xA1 || trail > 0xFD)
+				idx = 0;
+			else
+				idx = (lead - 0x88) * 93 + (trail - 0xA1);
+			break;
+		}
 		idx = ((idx % 256) - 0xa1)* 94  + ((idx / 256) - 0xa1);
 		break;
 	default:
 		idx = 0;
 	}
+	// 繁中化：畫指令列時改從 verb 字型取字模。兩份檔的尺寸與索引完全一致，
+	// 所以只是換一個基底指標，其餘計算共用。
+	if (_useVerbFont && _2byteVerbFontPtr)
+		return _2byteVerbFontPtr + ((_2byteVerbWidth + 7) / 8) * _2byteVerbHeight * idx;
+	if (_useDialogFont && _2byteDialogFontPtr)
+		return _2byteDialogFontPtr + ((_2byteDialogWidth + 7) / 8) * _2byteDialogHeight * idx;
 	return	_2byteFontPtr + ((_2byteWidth + 7) / 8) * _2byteHeight * idx;
 }
 
@@ -451,6 +571,19 @@ int CharsetRendererCommon::getFontHeight() const {
 	} else if (_vm->_isIndy4Jap) {
 		return _vm->_force2ByteCharHeight ? 14 : _fontHeight;
 	} else if (_vm->_useCJKMode && !isSegaCD) {
+		// 繁中化（Maniac Mansion V1/V2 hi-res）：這裡回的是**邏輯**行高。字模畫在 2 倍的
+		// 文字表面上，所以邏輯高 = 字模高 / 2：16x15 → 7（不足原版 8，取 8）；
+		// 24x24 → 12，但方塊字上下貼在一起太擠，補 2 個邏輯像素的行距成 14。
+		// （比照 CharsetRendererTownsV3::getFontHeight() 在 CJK 下的做法。）
+		if (_vm->_game.id == GID_MANIAC && _vm->_game.version <= 2) {
+			int h = _vm->_2byteHeight / _vm->_textSurfaceMultiplier;
+			return MAX(h >= 12 ? h + 2 : h, _fontHeight);
+		}
+		// 繁中化（瘋狂時代 hi-res）：同理回**邏輯**行高。24x24 的字模在 2 倍表面上
+		// 只佔 12 個邏輯像素，行距補 1 成 13，與原版 12x12 的 (_2byteHeight + 1) 同級，
+		// 這樣對白換行與指令列的列距都不必另外調整。
+		if (_vm->isChtHiResCJK())
+			return MAX(_vm->_2byteHeight / _vm->_textSurfaceMultiplier + 1, _fontHeight);
 		return MAX(_vm->_2byteHeight + 1, _fontHeight);
 	} else {
 		return _fontHeight;
@@ -462,7 +595,9 @@ int CharsetRendererClassic::getCharWidth(uint16 chr) const {
 	int spacing = 0;
 
 	if (_vm->_useCJKMode && chr >= 0x80)
-		return _vm->_2byteWidth / 2;
+		// 繁中化（hi-res CJK）：回邏輯寬的一半，與非 hi-res 時的語意一致
+		return _vm->isChtHiResCJK() ? _vm->_2byteWidth / 2 / _vm->_textSurfaceMultiplier
+		                            : _vm->_2byteWidth / 2;
 
 	int offs = READ_LE_UINT32(_fontPtr + chr * 4 + 4);
 	if (offs)
@@ -560,7 +695,11 @@ int CharsetRenderer::getStringWidth(int arg, const byte *text) {
 						width += _vm->_2byteWidth;
 					}
 				} else {
-					width += _vm->_2byteWidth;
+					// 繁中化（hi-res CJK）：這裡累加的是**邏輯**寬。字模畫在 2 倍的文字表面上，
+					// 所以 24x24 的字在版面上只佔 12 個邏輯像素；直接加 _2byteWidth 會讓
+					// 換行與置中都以兩倍寬計算，長句提早折行、短句偏移。
+					width += _vm->isChtHiResCJK() ? _vm->_2byteWidth / _vm->_textSurfaceMultiplier
+					                              : _vm->_2byteWidth;
 					// Original keeps glyph width and character dimensions separately
 					if (_vm->_language == Common::KO_KOR || _vm->_language == Common::ZH_TWN) {
 						width++;
@@ -865,10 +1004,19 @@ void CharsetRendererPC::drawBits1Kor(Graphics::Surface &dest, int x1, int y1, co
 }
 
 int CharsetRendererV3::getDrawWidthIntern(uint16 chr) {
+	// 繁中化：這裡回的是**字模**寬（給 drawBits1 用），CJK 是 16；邏輯推進寬仍由
+	// getCharWidth() 回 8，printChar() 會把 origWidth 除以 _textSurfaceMultiplier
+	// 之後才推進 _left。
+	if (chr >= 256 && _vm->_useCJKMode)
+		return _vm->_2byteWidth;
 	return getCharWidth(chr);
 }
 
-int CharsetRendererV3::getDrawHeightIntern(uint16) {
+int CharsetRendererV3::getDrawHeightIntern(uint16 chr) {
+	// 繁中化：同上，這裡是**字模**高。原本固定回 8 會把中文字截成上半段，
+	// 畫面上看起來像橫條噪點而不是「字太小」。
+	if (chr >= 256 && _vm->_useCJKMode)
+		return _vm->_2byteHeight;
 	return 8;
 }
 
@@ -981,7 +1129,19 @@ void CharsetRendererV3::printChar(int chr, bool ignoreCharsetMask) {
 		_textScreenID = vs->number;
 	}
 
-	if ((ignoreCharsetMask || !vs->hasTwoBuffers)
+	// 繁中化（hi-res）：文字表面放大 2 倍時，**所有**文字都畫到文字表面上。
+	//
+	// 原本 hasTwoBuffers == false 的畫面（指令列／物品欄／句子列所在的
+	// kVerbVirtScreen、片頭字幕的 kTextVirtScreen）會把字直接畫進 1 倍的虛擬螢幕。
+	// 若只把中文導到 2 倍的文字表面、ASCII 留在虛擬螢幕，兩邊的清除時機就不一致，
+	// 畫面上會出現前後兩則訊息疊字。全部走文字表面則清除路徑統一
+	// （restoreBackground() 本來就會按 _textSurfaceMultiplier 一起清文字表面），
+	// 而 ASCII 由 drawBits1() 既有的 scale2x 自動放大 2 倍。這與 FM-Towns 把
+	// 文字全部放在上層的作法一致。
+	const bool hiResText = (_vm->_textSurfaceMultiplier == 2 &&
+							_vm->_game.id == GID_MANIAC && _vm->_game.version <= 2);
+
+	if ((ignoreCharsetMask || !vs->hasTwoBuffers) && !hiResText
 #ifndef DISABLE_TOWNS_DUAL_LAYER_MODE
 		&& (_vm->_game.platform != Common::kPlatformFMTowns)
 #endif
@@ -1208,6 +1368,14 @@ void CharsetRendererClassic::printChar(int chr, bool ignoreCharsetMask) {
 	if ((_vm->_language == Common::ZH_TWN || _vm->_language == Common::KO_KOR) && is2byte)
 		_origWidth++;
 
+	// 繁中化（瘋狂時代 hi-res）：畫用字模尺寸，推進與 _str 邊界用**邏輯**尺寸。
+	// prepareDraw() 把 _origWidth/_origHeight 設成字模的 24x24，這裡除回 12x12，
+	// 否則游標推進兩倍寬、一行只放得下半數的字。
+	if (is2byte && _vm->isChtHiResCJK()) {
+		_origWidth /= _vm->_textSurfaceMultiplier;
+		_origHeight /= _vm->_textSurfaceMultiplier;
+	}
+
 	_left += _origWidth;
 	if (is2byte)
 		_left += _cjkSpacing;
@@ -1295,9 +1463,24 @@ void CharsetRendererClassic::printCharIntern(bool is2byte, const byte *charPtr,
 			drawTop = _top - _vm->_screenTop;
 		}
 
-		if (is2byte && _vm->_game.platform != Common::kPlatformFMTowns)
-			drawBits1(dstSurface, (ignoreCharsetMask || !vs->hasTwoBuffers) ? _left + vs->xstart : _left, drawTop, charPtr, drawTop, origWidth, origHeight);
-		else
+		if (is2byte && _vm->_game.platform != Common::kPlatformFMTowns) {
+			// 繁中化（瘋狂時代 hi-res）：中文字模是 24x24，畫進 1 倍的虛擬螢幕會蓋掉兩倍
+			// 面積的畫面。改成一律畫到 2 倍的文字表面，座標跟著乘 _textSurfaceMultiplier；
+			// 合成由 drawStripToScreen() 負責（它會把底圖也放大 2 倍）。
+			// y 用 (_top - _screenTop) 與同函式上面算 dstPtr 的方式一致。
+			if (_vm->isChtHiResCJK()) {
+				// y 要跟原路徑畫到的**絕對**位置一致：走 vs 分支時原本畫在
+				// vs 的 (drawTop = _top - vs->topline)，換算回整個畫面就是 _top；
+				// 只有走文字表面分支（房間畫面、可能有垂直捲動）才扣 _screenTop。
+				// 一開始一律扣 _screenTop，指令列的中文就整批往上偏了 7 個像素，
+				// 疊到句子列上、又被句子列的重畫擦掉，看起來像「使用」那格消失。
+				const int m = _vm->_textSurfaceMultiplier;
+				const int surfY = (ignoreCharsetMask || !vs->hasTwoBuffers) ? _top : (_top - _vm->_screenTop);
+				drawBits1(_vm->_textSurface, _left * m, surfY * m, charPtr, drawTop, origWidth, origHeight);
+			} else {
+				drawBits1(dstSurface, (ignoreCharsetMask || !vs->hasTwoBuffers) ? _left + vs->xstart : _left, drawTop, charPtr, drawTop, origWidth, origHeight);
+			}
+		} else
 			drawBitsN(dstSurface, dstPtr, charPtr, *_fontPtr, drawTop, origWidth, origHeight);
 
 		if (_blitAlso && vs->hasTwoBuffers) {
diff --git a/engines/scumm/dialogs.cpp b/engines/scumm/dialogs.cpp
index 121e9835..a1f67f6b 100644
--- a/engines/scumm/dialogs.cpp
+++ b/engines/scumm/dialogs.cpp
@@ -782,6 +782,20 @@ const ResString &InfoDialog::getStaticResString(Common::Language lang, int strin
 	bool useHardcodedV3QuitPrompt = stringno == 5 && _vm->_game.version == 3 && _vm->_game.id != GID_LOOM;
 	bool useFixedDottMenuStrings = stringno >= 21 && stringno <= 26 && _vm->_game.id == GID_TENTACLE;
 
+	// 繁中化（Maniac Mansion V2）：v1/v2 的暫停／重新開始／離開這三則訊息，各語言版本
+	// 是各自寫死在直譯器裡的，官方沒有中文版。這裡補上，位元組是本專案自訂碼空間
+	// （首碼 0x88-0x9F、尾碼 0xA1-0xFD），要配 game 夾裡的 chinese_gb16x12.fnt 才畫得出來，
+	// 所以限定 maniac v2 + ZH_CHN 才生效。
+	// 結尾的 "(y/n)y" 保持 ASCII：引擎是拿字串最後一個字元當「確定」鍵。
+	static const ResString strMapZhManiacV2[] = {
+		{4, """\x8e\xbd\x8e\xbe\x92\xe7\x8d\xfd\x88\xac\x8a\xc7\x88\xe9\x88\xea\x8d\xb6\x92\xe8\x92\xe9\x88\xbf"""},                       // 遊戲暫停，按空白鍵繼續。
+		{5, """\x8c\xf7\x8c\xd3\x8a\xb9\x8d\xd3\x90\xfc\x89\xde\x8d\xfc\x89\xef\x89\xd2""(y/n)y"},   // 確定要重新開始嗎？(y/n)y
+		{6, """\x8c\xf7\x8c\xd3\x8a\xb9\x8a\xb5\x89\xde\x89\xef\x89\xd2""(y/n)y"}                    // 確定要離開嗎？(y/n)y
+	};
+	if (lang == Common::ZH_CHN && _vm->_game.id == GID_MANIAC && _vm->_game.version <= 2 &&
+		stringno >= 3 && stringno <= 5)
+		return strMapZhManiacV2[stringno - 3];
+
 	// I have added the languages I found in scumm-md5.h for v1/2 games...
 	int langIndex = 0;
 	switch (lang) {
diff --git a/engines/scumm/gfx.cpp b/engines/scumm/gfx.cpp
index bf85f13c..94a3a064 100644
--- a/engines/scumm/gfx.cpp
+++ b/engines/scumm/gfx.cpp
@@ -704,6 +704,44 @@ void ScummEngine::drawStripToScreen(VirtScreen *vs, int x, int width, int top, i
 			return;
 		} else
 #endif
+		// 繁中化（Maniac Mansion V2）：hi-res 文字表面的合成。
+		//
+		// 底下原本的合成迴圈會跑 (height*m) x (width*m) 個目的像素，但 src 是**線性**
+		// 讀取 1 倍的底圖 —— m=2 時底圖整片錯位，畫面變成雪花。這正是「DOS 版不能設
+		// _textSurfaceMultiplier=2」這個結論的成因：缺的其實只是「底圖也要放大」一步。
+		//
+		// 補上之後就等同 SCI 引擎的 GFX_SCREEN_UPSCALED_640x400：原畫 2x nearest 放大
+		// （不動原始美術），中文字直接畫在 2 倍的文字表面上，繞過整體 nearest 放大，
+		// 因此同一畫面「美術照原樣、中文銳利」。
+		//
+		// 注意：這條路徑直接送 copyRectToScreen，跳過 postProcessDOSGraphics()，
+		// 因此與 CGA / Hercules 這類需要後處理的 render mode 不相容（預設 EGA/VGA 下
+		// 那個函式本來就會立刻 return，所以沒有差別）。
+		if (m == 2 && _useCJKMode && _outputPixelFormat.bytesPerPixel == 1 &&
+			((_game.id == GID_MANIAC && _game.version <= 2) || isChtHiResCJK())) {
+			const byte *srcRow = (const byte *)src;
+			const byte *textRow = (const byte *)_textSurface.getBasePtr(x * m, y * m);
+			byte *dstPtr = _compositeBuf;
+
+			for (int h = 0; h < height * m; ++h) {
+				const byte *s = srcRow;
+				const byte *t = textRow;
+				for (int w = 0; w < width; ++w) {
+					byte bg = *s++;
+					for (int sub = 0; sub < m; ++sub) {
+						byte tx = *t++;
+						*dstPtr++ = (tx == CHARSET_MASK_TRANSPARENCY) ? bg : tx;
+					}
+				}
+				if (h & 1)
+					srcRow += vs->pitch;   // 每兩個目的列才前進一個來源列
+				textRow += _textSurface.pitch;
+			}
+
+			_system->copyRectToScreen(_compositeBuf, width * m, x * m, y * m, width * m, height * m);
+			return;
+		}
+
 		// Compose the text over the game graphics
 		if (_outputPixelFormat.bytesPerPixel == 2) {
 			const byte *srcPtr = (const byte *)src;
@@ -1265,7 +1303,12 @@ void ScummEngine::restoreBackground(Common::Rect rect, byte backColor) {
 		}
 #endif
 
-		if (_macScreen) {
+		// 繁中化（hi-res）：跟 restoreCharsetBg() 同一類問題。沒有雙緩衝的虛擬螢幕
+		// （指令列）原本靠下面那個 fill 把文字一起蓋掉，但 hi-res 模式的中文是畫在
+		// 文字表面上的，fill 不到 —— 句子列重畫時上一句就留在畫面上（issue #1：
+		// 暫停訊息疊在句子上）。這裡把對應的那一塊文字表面一起清掉。
+		if (_macScreen || (_textSurfaceMultiplier == 2 && _useCJKMode &&
+						   ((_game.id == GID_MANIAC && _game.version <= 2) || isChtHiResCJK()))) {
 			byte *mask = (byte *)_textSurface.getBasePtr(rect.left * _textSurfaceMultiplier, (rect.top + vs->topline) * _textSurfaceMultiplier);
 			fill(mask, _textSurface.pitch, CHARSET_MASK_TRANSPARENCY, width * _textSurfaceMultiplier, height * _textSurfaceMultiplier, _textSurface.format.bytesPerPixel);
 		}
@@ -1320,9 +1363,36 @@ void ScummEngine::restoreCharsetBg() {
 			}
 		}
 
-		if (vs->hasTwoBuffers || _macScreen) {
+		if (isChtHiResCJK()) {
+			// 繁中化（瘋狂時代 hi-res）：文字表面是整個畫面共用的，而 clearTextSurface()
+			// 清的是整片。主畫面（有雙緩衝）每重畫一次就把指令區那塊也清掉，
+			// 而 v6 的 verb 只在狀態改變時才重畫 —— 於是指令列的中文被清掉之後
+			// 再也不會回來，畫面上看到的只是先前合成留在螢幕上的殘影；
+			// 一旦某格所在的 strip 因為句子列更新而重新合成，那一格就空了
+			// （症狀：只有與句子列橫向重疊的「使用」那格消失）。
+			// 這裡改成只清當前虛擬螢幕對應的那一塊，各畫面互不干擾。
+			byte *mask = (byte *)_textSurface.getBasePtr(0, vs->topline * _textSurfaceMultiplier);
+			fill(mask, _textSurface.pitch, CHARSET_MASK_TRANSPARENCY,
+				 vs->w * _textSurfaceMultiplier, vs->h * _textSurfaceMultiplier,
+				 _textSurface.format.bytesPerPixel);
+		} else if (vs->hasTwoBuffers || _macScreen) {
 			// Clean out the charset mask
 			clearTextSurface();
+		} else if (_textSurfaceMultiplier == 2 &&
+				   (_game.id == GID_MANIAC && _game.version <= 2)) {
+			// 註：這段對 GID_TENTACLE 不適用。v2 的指令列每幀重畫，清掉整塊文字表面
+			// 之後下一幀就補回來；v6 的 verb 只在狀態改變時重畫，整塊清掉就再也不會回來
+			// （症狀是指令列的某幾格憑空消失）。v6 用的是下面 restoreBackground() 裡
+			// 那段依 rect 清除的路徑。
+			// 繁中化（hi-res）：上面那段 clearTextSurface() 只在 hasTwoBuffers 時執行，
+			// 因為原本設計裡 kTextVirtScreen / kVerbVirtScreen 的文字是直接畫進虛擬螢幕、
+			// 由前面的 memset 清掉的。hi-res 模式把所有文字都畫到文字表面之後，這些畫面
+			// 就沒人清了，畫面上會出現前後兩則訊息疊字。這裡只清該虛擬螢幕對應的那一塊，
+			// 不動其他畫面（例如指令列）已經畫好的文字。
+			byte *mask = (byte *)_textSurface.getBasePtr(0, vs->topline * _textSurfaceMultiplier);
+			fill(mask, _textSurface.pitch, CHARSET_MASK_TRANSPARENCY,
+				 vs->w * _textSurfaceMultiplier, vs->h * _textSurfaceMultiplier,
+				 _textSurface.format.bytesPerPixel);
 		}
 	}
 }
diff --git a/engines/scumm/gfx_gui.cpp b/engines/scumm/gfx_gui.cpp
index 133f6ec0..73ed255e 100644
--- a/engines/scumm/gfx_gui.cpp
+++ b/engines/scumm/gfx_gui.cpp
@@ -412,7 +412,7 @@ Common::KeyState ScummEngine::printMessageAndPause(const char *msg, int color, i
 			sentenceline.right = _virtscr[kVerbVirtScreen].w - 1;
 		} else {
 			sentenceline.top = _virtscr[kVerbVirtScreen].topline + pixelYOffset;
-			sentenceline.bottom = _virtscr[kVerbVirtScreen].topline + 8 + pixelYOffset;
+			sentenceline.bottom = _virtscr[kVerbVirtScreen].topline + getSentenceLineHeight() + pixelYOffset;
 			sentenceline.left = 0 + pixelXOffset;
 			sentenceline.right = _virtscr[kVerbVirtScreen].w - 1 + pixelXOffset;
 		}
diff --git a/engines/scumm/input.cpp b/engines/scumm/input.cpp
index ecef09cf..0e5d11d6 100644
--- a/engines/scumm/input.cpp
+++ b/engines/scumm/input.cpp
@@ -117,6 +117,13 @@ void ScummEngine::parseEvent(Common::Event event) {
 
 	switch (event.type) {
 	case Common::EVENT_CUSTOM_ENGINE_ACTION_START:
+		// 繁中化：語音包切換是一次性動作，在事件當下做完就好，不必像移動類動作
+		// 那樣記進 _actionMap 等主迴圈輪詢。
+		if (event.customType == kScummActionChtVoicePack) {
+			if (_sound)
+				_sound->cycleChtVoicePack();
+			break;
+		}
 		if (event.customType >= kScummActionCount) {
 			debugC(DEBUG_GENERAL, "customType >= kScummActionCount (%d)", event.customType);
 		} else {
diff --git a/engines/scumm/metaengine.cpp b/engines/scumm/metaengine.cpp
index d69986ea..cee5a900 100644
--- a/engines/scumm/metaengine.cpp
+++ b/engines/scumm/metaengine.cpp
@@ -1339,6 +1339,21 @@ Common::KeymapArray ScummMetaEngine::initKeymaps(const char *target) const {
 		keymaps.push_back(rebel2Keymap);
 	}
 
+	// 繁中化：DOTT 的語音包切換（原版英文／台式中文／原音克隆中文）。掛在
+	// engine-default 上，玩家可以在 ScummVM 的按鍵設定裡改成別的鍵。Ctrl+T
+	// 目前在 SCUMM、全域與圖形後端都沒有人用。
+	if (gameId == "tentacle") {
+		for (uint i = 0; i < keymaps.size(); ++i) {
+			if (keymaps[i]->getId() != "engine-default")
+				continue;
+			act = new Action("CHTVOICE", _("Switch voice pack"));
+			act->setCustomEngineActionEvent(kScummActionChtVoicePack);
+			act->addDefaultInputMapping("C+t");
+			keymaps[i]->addAction(act);
+			break;
+		}
+	}
+
 	return keymaps;
 }
 
diff --git a/engines/scumm/script_v2.cpp b/engines/scumm/script_v2.cpp
index 241d938a..bde68828 100644
--- a/engines/scumm/script_v2.cpp
+++ b/engines/scumm/script_v2.cpp
@@ -373,6 +373,18 @@ void ScummEngine_v2::decodeParseString() {
 
 	while ((c = fetchScriptByte())) {
 
+		// 繁中化：自訂碼空間的首碼落在 0x88-0x9F，該位元組與下一個原樣寫入。
+		//
+		// v2 的腳本字串用「位元組 | 0x80」表示「該位元組後面接一個空白」（空白壓縮），
+		// 底下的 `c &= 0x7f` 會把任何高位元砍掉，中文因此完全進不來。首碼避開了所有
+		// 壓縮得出來的值：可列印 ASCII (0x20-0x7E) | 0x80 = 0xA0-0xFE，以及控制碼
+		// 0x01-0x03 | 0x80 = 0x81-0x83（片頭字幕 `by\001     Ron` 就會壓出 0x81）。
+		if (_useCJKMode && c >= 0x88 && c <= 0x9F) {
+			*ptr++ = c;
+			*ptr++ = fetchScriptByte();
+			continue;
+		}
+
 		insertSpace = (c & 0x80) != 0;
 		c &= 0x7f;
 
@@ -895,6 +907,14 @@ void ScummEngine_v2::o2_verbOps() {
 			vs->curRect.top = y;
 		}
 
+		// 繁中化（24x24 字模）：指令區改成 14 個邏輯像素一列，起點也從 144 下移到 156
+		// （文字帶加高 12 的關係，見 init()）。腳本給的是 8 格制的絕對座標
+		// （句子列 144、指令三列 152/160/168），這裡換算成 156 起算的 14 格制。
+		if (_useCJKMode && _2byteHeight == 24 && _game.id == GID_MANIAC && _game.version <= 2 &&
+			vs->curRect.top >= 144 && vs->curRect.top < 200) {
+			vs->curRect.top = 156 + (vs->curRect.top - 144) * 7 / 4;
+		}
+
 #ifdef USE_TTS
 		if (_game.id == GID_MANIAC && (_game.features & GF_DEMO)) {
 			ttsAction = Common::TextToSpeechManager::QUEUE;
diff --git a/engines/scumm/scumm.cpp b/engines/scumm/scumm.cpp
index cb0f5bc1..d49abf5a 100644
--- a/engines/scumm/scumm.cpp
+++ b/engines/scumm/scumm.cpp
@@ -1268,6 +1268,22 @@ Common::Error ScummEngine::init() {
 	// Load it earlier so _useCJKMode variable could be set
 	loadCJKFont();
 
+	// 繁中化（Maniac Mansion V2，24x24 字模）：指令區要放 6 列文字
+	// （句子列 1 + 指令 3 + 物品欄 2），一列 14 個邏輯像素（字模 12 + 2 的行距）
+	// 共 84，而原版的 144..200 只有 56。
+	//
+	// 解法是**把畫布加高**，不是把房間或上方的文字帶裁掉。
+	//
+	// 上面那條文字帶也要跟著加高：它原本是 16 像素 = 剛好兩行 8 像素的字，
+	// 而 CJK 的行高變成 14，兩行要 28。不加高的話第二行會畫到帶子外面，
+	// 而 restoreCharsetBg() 只清帶子那麼高，於是上一則訊息的殘影留在下面，
+	// 新的字疊上去就糊成一團（片頭的製作名單第二行就是這樣壞的）。
+	//
+	// 畫面總高 200 → 240 = 文字帶 28 + 房間 128 + 指令區 84。
+	// 這一步必須在下面的 initGraphics 之前做。
+	if (_game.id == GID_MANIAC && _game.version <= 2 && _useCJKMode && _2byteHeight == 24)
+		_screenHeight = 240;
+
 #ifndef DISABLE_TOWNS_DUAL_LAYER_MODE
 	if (_game.platform == Common::kPlatformFMTowns && _forceFMTownsHiResMode)
 		_textSurfaceMultiplier = 2;
@@ -2068,6 +2084,10 @@ void ScummEngine::resetScumm() {
 		initScreens(8, 144);
 	} else if ((_game.id == GID_MANIAC) && (_game.version <= 1) && !(_game.platform == Common::kPlatformNES)) {
 		initScreens(16, 152);
+	} else if (_game.id == GID_MANIAC && _game.version <= 2 && _useCJKMode && _2byteHeight == 24) {
+		// 繁中化（24x24）：文字帶 0..28、房間 28..156（仍是完整的 128 列）、
+		// 指令區 156..240（6 列 x 14）。
+		initScreens(28, 156);
 	} else if (_game.version >= 7 || _game.heversion >= 71) {
 		initScreens(0, _screenHeight);
 	} else if ((_game.id == GID_ZAK) && (_game.platform == Common::kPlatformC64)) {
diff --git a/engines/scumm/scumm.h b/engines/scumm/scumm.h
index 9864f7e9..10f20ebe 100644
--- a/engines/scumm/scumm.h
+++ b/engines/scumm/scumm.h
@@ -502,6 +502,9 @@ enum ScummAction {
 	kScummActionInsaneBack,
 	kScummActionInsaneSkip,
 
+	// 繁中化：遊戲中循環切換語音包（原版英文／台式中文／原音克隆中文）
+	kScummActionChtVoicePack,
+
 	kScummActionCount
 };
 
@@ -1778,8 +1781,34 @@ public:
 	bool isScummvmKorTarget();
 	bool hasLocalizer();
 
+	// 繁中化（瘋狂時代，24x24）：hi-res CJK 文字表面是否生效。
+	// 生效時字模畫在 2 倍的文字表面上，而排版（推進寬、行高、dirty rect）
+	// 一律仍以**邏輯像素**計，也就是字模尺寸 / _textSurfaceMultiplier。
+	bool isChtHiResCJK() const {
+		return _textSurfaceMultiplier == 2 && _useCJKMode && _game.id == GID_TENTACLE;
+	}
+
+	// 繁中化（Maniac Mansion V2，24x24）：句子列本來固定 8 條掃描線，中文字有 14 條。
+	// 重畫句子列前要清掉的是**上一句實際佔的高度**，用 8 的話下面 6 條會留著上一句的殘影
+	// （最明顯的是暫停訊息疊在句子上，見 issue #1）。
+	int getSentenceLineHeight() const {
+		if (_useCJKMode && _2byteHeight == 24 && _game.id == GID_MANIAC && _game.version <= 2)
+			return _2byteHeight / _textSurfaceMultiplier + 2;
+		return 8;
+	}
+
 //protected:
 	byte *_2byteFontPtr = nullptr;
+	// 繁中化：指令列專用字型（遊戲夾裡的 chinese_verb.fnt），與正文字型同尺寸同索引。
+	// _useVerbFont 只在 drawVerb() 畫字的那一小段為真。
+	byte *_2byteVerbFontPtr = nullptr;
+	int _2byteVerbWidth = 0, _2byteVerbHeight = 0;
+	bool _useVerbFont = false;
+	// 繁中化：對白專用字型（chinese_dialog.fnt）。對白畫在遊戲畫面上，沒有指令列
+	// 那種列距限制，所以可以放大；句子列與其他 UI 字串仍用正文字型。
+	byte *_2byteDialogFontPtr = nullptr;
+	int _2byteDialogWidth = 0, _2byteDialogHeight = 0;
+	bool _useDialogFont = false;
 	byte *_2byteMultiFontPtr[20];
 	int _2byteMultiHeight[20];
 	int _2byteMultiWidth[20];
diff --git a/engines/scumm/sound.cpp b/engines/scumm/sound.cpp
index bfb33823..94daba01 100644
--- a/engines/scumm/sound.cpp
+++ b/engines/scumm/sound.cpp
@@ -20,6 +20,7 @@
  */
 
 #include "common/config-manager.h"
+#include "common/system.h"
 #include "common/util.h"
 #include "common/ptr.h"
 #include "common/substream.h"
@@ -1030,6 +1031,14 @@ void Sound::talkSound(uint32 offset, uint32 length, int mode, int channel) {
 }
 
 void Sound::setupSound() {
+	// 繁中化：記住玩家上次選的語音包。setupSound() 是 setupSfxFile() 的第一個
+	// 呼叫點，在這裡讀比在建構子讀更貼近實際使用的時機。
+	if (ConfMan.hasKey("cht_voice_pack")) {
+		int pack = ConfMan.getInt("cht_voice_pack");
+		if (pack > kChtVoiceOriginal && pack < kChtVoicePackCount)
+			_chtVoicePack = pack;
+	}
+
 	setupSfxFile();
 
 	if (_vm->_game.id == GID_FT) {
@@ -1151,6 +1160,31 @@ void Sound::setupSfxFile() {
 
 		_soundMode = kVOCMode;
 	} else {
+		// 繁中化：語音包切換。中文語音是另外的檔（monster-tw.* / monster-cl.*），
+		// 索引表的 org_offset 與原版完全一致，所以換檔就等於換語音，腳本與其他
+		// 資料一個位元組都不用動。找不到就靜靜回退到原版，玩家沒放中文語音時
+		// 一切照舊。
+		static const char *const chtSuffix[] = { "", "-tw", "-cl" };
+		if (_chtVoicePack > kChtVoiceOriginal && _chtVoicePack < kChtVoicePackCount) {
+			for (uint j = 0; j < 2 && !file->isOpen(); ++j) {
+				Common::String stem(basename[j]);
+				stem.deleteLastChar();          // 去掉結尾的 '.'
+				stem += chtSuffix[_chtVoicePack];
+				stem += '.';
+				for (int i = 0; extensions[i].ext; ++i) {
+					tmp = stem;
+					tmp.appendInPlace(extensions[i].ext);
+					if (_vm->openFile(*file, tmp)) {
+						_soundMode = extensions[i].mode;
+						_sfxFilename = tmp.toString('/');
+						break;
+					}
+				}
+			}
+			if (!file->isOpen())
+				_chtVoicePack = kChtVoiceOriginal;
+		}
+
 		for (uint j = 0; j < 2 && !file->isOpen(); ++j) {
 			for (int i = 0; extensions[i].ext; ++i) {
 				tmp = basename[j];
@@ -1209,6 +1243,35 @@ void Sound::setupSfxFile() {
 	delete file;
 }
 
+// 繁中化：循環切換語音包。要先把正在播的語音停掉——舊的 _offsetTable 一釋放，
+// 還在解碼的串流就會指到已經釋放的記憶體。
+void Sound::cycleChtVoicePack() {
+	stopTalkSound();
+	_mixer->stopHandle(*_talkChannelHandle);
+
+	free(_offsetTable);
+	_offsetTable = nullptr;
+	_numSoundEffects = 0;
+
+	_chtVoicePack = (_chtVoicePack + 1) % kChtVoicePackCount;
+	int wanted = _chtVoicePack;
+	setupSfxFile();
+
+	ConfMan.setInt("cht_voice_pack", _chtVoicePack);
+	ConfMan.flushToDisk();
+
+	static const char *const names[] = { "Original (English)", "Taiwanese Mandarin", "Cloned voices" };
+	Common::U32String msg;
+	if (_chtVoicePack != wanted) {
+		// setupSfxFile() 找不到該語音包時會把 _chtVoicePack 退回原版
+		msg = Common::U32String::format("Voice pack %s not found - using %s",
+										names[wanted], names[_chtVoicePack]);
+	} else {
+		msg = Common::U32String::format("Voice: %s", names[_chtVoicePack]);
+	}
+	g_system->displayMessageOnOSD(msg);
+}
+
 bool Sound::isSfxFinished() const {
 	return !_mixer->hasActiveChannelOfType(Audio::Mixer::kSFXSoundType);
 }
diff --git a/engines/scumm/sound.h b/engines/scumm/sound.h
index 2b9b1e66..d946baad 100644
--- a/engines/scumm/sound.h
+++ b/engines/scumm/sound.h
@@ -185,7 +185,20 @@ public:
 		_soundSE->setupMISEAudioParams(scriptNum, scriptOffset);
 	}
 
+	// 繁中化：語音包切換。三份語音檔的差別只有音訊本身，索引表的 org_offset
+	// 完全一致（腳本不用改），所以切換等於換一個檔名再重讀一次索引。
+	enum ChtVoicePack {
+		kChtVoiceOriginal = 0,   // 原版英文（monster.sof）
+		kChtVoiceTaiwanese,      // 台式中文 TTS（monster-tw.sof）
+		kChtVoiceCloned,         // 原音克隆中文（monster-cl.sof）
+		kChtVoicePackCount
+	};
+	void cycleChtVoicePack();
+	int getChtVoicePack() const { return _chtVoicePack; }
+
 protected:
+	int _chtVoicePack = kChtVoiceOriginal;
+
 	void setupSfxFile();
 	bool isSfxFinished() const;
 	void processSfxQueues();
diff --git a/engines/scumm/string.cpp b/engines/scumm/string.cpp
index ebb998ec..00eec0dc 100644
--- a/engines/scumm/string.cpp
+++ b/engines/scumm/string.cpp
@@ -1015,7 +1015,7 @@ void ScummEngine_v2::drawSentence() {
 		sentenceline.right = _virtscr[kVerbVirtScreen].w - 1;
 	} else {
 		sentenceline.top = _virtscr[kVerbVirtScreen].topline + pixelYOffset;
-		sentenceline.bottom = _virtscr[kVerbVirtScreen].topline + 8 + pixelYOffset;
+		sentenceline.bottom = _virtscr[kVerbVirtScreen].topline + getSentenceLineHeight() + pixelYOffset;
 		sentenceline.left = 0 + pixelXOffset;
 		sentenceline.right = _virtscr[kVerbVirtScreen].w - 1 + pixelXOffset;
 	}
@@ -1030,7 +1030,33 @@ void ScummEngine_v2::drawSentence() {
 	drawString(2, (byte *)string);
 }
 
+// 繁中化：對白期間換上較大的字型。displayDialog() 有五個 return，用 RAII
+// 包住才不會有哪條路徑忘了還原。寬高一起換，斷行與座標計算才會跟著新尺寸走。
+namespace {
+struct ChtDialogFont {
+	ScummEngine *_vm;
+	int _w, _h;
+	bool _on;
+	explicit ChtDialogFont(ScummEngine *vm) : _vm(vm), _w(vm->_2byteWidth), _h(vm->_2byteHeight), _on(false) {
+		if (vm->_2byteDialogFontPtr) {
+			vm->_useDialogFont = true;
+			vm->_2byteWidth = vm->_2byteDialogWidth;
+			vm->_2byteHeight = vm->_2byteDialogHeight;
+			_on = true;
+		}
+	}
+	~ChtDialogFont() {
+		if (_on) {
+			_vm->_useDialogFont = false;
+			_vm->_2byteWidth = _w;
+			_vm->_2byteHeight = _h;
+		}
+	}
+};
+} // End of anonymous namespace
+
 void ScummEngine::displayDialog() {
+	ChtDialogFont chtFont(this);
 	Actor *a;
 	if (_game.heversion >= 60 && _haveMsg == 3) {
 		stopTalk();
diff --git a/engines/scumm/verbs.cpp b/engines/scumm/verbs.cpp
index 1637dd33..705f28cc 100644
--- a/engines/scumm/verbs.cpp
+++ b/engines/scumm/verbs.cpp
@@ -231,13 +231,21 @@ void ScummEngine_v2::initV2MouseOver() {
 	int pixelYOffset = (_game.platform == Common::kPlatformC64) ? 1 : 0;
 	int pixelXOffset = (_game.platform == Common::kPlatformC64) ? 1 : 0;
 
+	// 繁中化（24x24 字模）：指令區的一列從 8 個邏輯像素變成 14（字模 12 + 2 行距），
+	// 物品欄因此從相對 32 移到 56（句子列 14 + 指令三列 42）。
+	// 這些矩形同時是**點擊判定區**，所以改了之後滑鼠命中會自己跟著走。
+	// 列高一律由 getSentenceLineHeight() 給（另外三處：checkExecVerbs、checkV2Inventory、
+	// redrawV2Inventory 也用同一個來源——這個值先前散在四個地方，漏改一處就是 issue #1）。
+	const int rowH = getSentenceLineHeight();
+	const int invTop = 4 * rowH;
+
 	// Inventory items
 
 	for (i = 0; i < 2; i++) {
 		_mouseOverBoxesV2[2 * i].rect.left = 0 + pixelXOffset;
 		_mouseOverBoxesV2[2 * i].rect.right = 144 + pixelXOffset;
-		_mouseOverBoxesV2[2 * i].rect.top = 32 + 8 * i + pixelYOffset;
-		_mouseOverBoxesV2[2 * i].rect.bottom = _mouseOverBoxesV2[2 * i].rect.top + 8;
+		_mouseOverBoxesV2[2 * i].rect.top = invTop + rowH * i + pixelYOffset;
+		_mouseOverBoxesV2[2 * i].rect.bottom = _mouseOverBoxesV2[2 * i].rect.top + rowH;
 
 		_mouseOverBoxesV2[2 * i].color = color;
 		_mouseOverBoxesV2[2 * i].hicolor = _hiLiteColorInvSentence;
@@ -255,16 +263,16 @@ void ScummEngine_v2::initV2MouseOver() {
 
 	_mouseOverBoxesV2[kInventoryUpArrow].rect.left = 144 + pixelXOffset;
 	_mouseOverBoxesV2[kInventoryUpArrow].rect.right = 176 + pixelXOffset;
-	_mouseOverBoxesV2[kInventoryUpArrow].rect.top = 32 + pixelYOffset;
-	_mouseOverBoxesV2[kInventoryUpArrow].rect.bottom = 40 + pixelYOffset;
+	_mouseOverBoxesV2[kInventoryUpArrow].rect.top = invTop + pixelYOffset;
+	_mouseOverBoxesV2[kInventoryUpArrow].rect.bottom = invTop + rowH + pixelYOffset;
 
 	_mouseOverBoxesV2[kInventoryUpArrow].color = arrow_color;
 	_mouseOverBoxesV2[kInventoryUpArrow].hicolor = _hiLiteColorVerbArrow;
 
 	_mouseOverBoxesV2[kInventoryDownArrow].rect.left = 144 + pixelXOffset;
 	_mouseOverBoxesV2[kInventoryDownArrow].rect.right = 176 + pixelXOffset;
-	_mouseOverBoxesV2[kInventoryDownArrow].rect.top = 40 + pixelYOffset;
-	_mouseOverBoxesV2[kInventoryDownArrow].rect.bottom = 48 + pixelYOffset;
+	_mouseOverBoxesV2[kInventoryDownArrow].rect.top = invTop + rowH + pixelYOffset;
+	_mouseOverBoxesV2[kInventoryDownArrow].rect.bottom = invTop + 2 * rowH + pixelYOffset;
 
 	_mouseOverBoxesV2[kInventoryDownArrow].color = arrow_color;
 	_mouseOverBoxesV2[kInventoryDownArrow].hicolor = _hiLiteColorVerbArrow;
@@ -274,7 +282,7 @@ void ScummEngine_v2::initV2MouseOver() {
 	_mouseOverBoxesV2[kSentenceLine].rect.left = 0 + pixelXOffset;
 	_mouseOverBoxesV2[kSentenceLine].rect.right = 320 + pixelXOffset;
 	_mouseOverBoxesV2[kSentenceLine].rect.top = 0 + pixelYOffset;
-	_mouseOverBoxesV2[kSentenceLine].rect.bottom = 8 + pixelYOffset;
+	_mouseOverBoxesV2[kSentenceLine].rect.bottom = rowH + pixelYOffset;
 
 	_mouseOverBoxesV2[kSentenceLine].color = color;
 	_mouseOverBoxesV2[kSentenceLine].hicolor = _hiLiteColorInvSentence;
@@ -401,7 +409,8 @@ void ScummEngine_v2::checkV2MouseOver(Common::Point pos) {
 
 int ScummEngine_v2::checkV2Inventory(int x, int y) {
 	bool isNES = (_game.platform == Common::kPlatformNES);
-	int inventoryArea = isNES ? 48 : 32;
+	// 繁中化（24x24）：物品欄的起點跟著 14 格制往下移（見 initV2MouseOver）
+	int inventoryArea = isNES ? 48 : 4 * getSentenceLineHeight();
 	int object = 0;
 
 	y -= _virtscr[kVerbVirtScreen].topline;
@@ -438,7 +447,13 @@ void ScummEngine_v2::redrawV2Inventory() {
 	int maxVisibleInv, invCount, obj;
 	Common::Rect inventoryBox;
 	bool isNES = (_game.platform == Common::kPlatformNES);
-	int inventoryArea = isNES ? 48 : 32;
+	// 繁中化（24x24）：物品欄的起點跟著 14 格制往下移，數值必須與 initV2MouseOver 的
+	// invTop 一致（句子列 14 + 指令三列 42 = 56）。這裡曾經寫成 48，比指令第三列的
+	// 底部（198+14=212，相對 56）高了 8 列，於是下面那個 restoreBackground() 會把
+	// 第三列指令的下半截一起清掉——原本看不出來，是因為清除只作用在虛擬螢幕緩衝區，
+	// 而中文是畫在文字表面上；等文字表面也一起清了（見 gfx.cpp restoreBackground），
+	// 「給／閱讀／查看／使用／修理」就少了一半。
+	int inventoryArea = isNES ? 48 : 4 * getSentenceLineHeight();
 	int maxChars = isNES ? 13 : 18;
 	int pixelYOffset = (_game.platform == Common::kPlatformC64) ? 1 : 0;
 	int pixelXOffset = (_game.platform == Common::kPlatformC64) ? 1 : 0;
@@ -485,6 +500,23 @@ void ScummEngine_v2::redrawV2Inventory() {
 		msg[maxChars] = 0;
 		strncpy((char *)msg, (const char *)tmp, maxChars);
 
+		// 繁中化：上面的 strncpy 是按 byte 截斷，可能把雙位元組字砍成半個
+		// （只留下首碼），畫面上會出現一個亂碼字。往前掃到最後一個完整的字。
+		if (_useCJKMode && _language == Common::ZH_CHN) {
+			int cut = 0;
+			for (int k = 0; msg[k]; ) {
+				if (msg[k] >= 0x88 && msg[k] <= 0x9F) {
+					if (!msg[k + 1])
+						break;
+					k += 2;
+				} else {
+					k += 1;
+				}
+				cut = k;
+			}
+			msg[cut] = 0;
+		}
+
 		// Draw it
 		drawString(1, msg);
 	}
@@ -755,13 +787,19 @@ void ScummEngine_v2::checkExecVerbs() {
 	} else if (_mouseAndKeyboardStat & MBS_MOUSE_MASK) {
 		VirtScreen *zone = findVirtScreen(_mouse.y);
 		const byte code = _mouseAndKeyboardStat & MBS_LEFT_CLICK ? 1 : 2;
-		const int inventoryArea = (_game.platform == Common::kPlatformNES) ? 48: 32;
+		// 繁中化（24x24 字模）：指令區一列是 14 個邏輯像素而不是 8，所以「句子列到哪裡結束」
+		// 與「物品欄從哪裡開始」都要跟著算（句子列 14 + 指令三列 42 = 56）。
+		// 這兩個數字原本寫死 8 / 32，結果第二、三列的點擊被當成物品欄點擊吃掉——
+		// hover 反白照常（那條路徑走 initV2MouseOver 的矩形，已經是 14 格制），
+		// 所以症狀是「滑過會變色、點下去沒反應」。
+		const int rowH = getSentenceLineHeight();
+		const int inventoryArea = (_game.platform == Common::kPlatformNES) ? 48 : 4 * rowH;
 
 		// Fixes bug #2773: "MANIACNES: Crash on click in speechtext-area"
 		if (!zone)
 			return;
 
-		if (zone->number == kVerbVirtScreen && _mouse.y <= zone->topline + 8) {
+		if (zone->number == kVerbVirtScreen && _mouse.y <= zone->topline + rowH) {
 			// Click into V2 sentence line
 			runInputScript(kSentenceClickArea, 0, 0);
 		} else if (zone->number == kVerbVirtScreen && _mouse.y >= zone->topline + inventoryArea) {
@@ -1178,7 +1216,29 @@ void ScummEngine::drawVerb(int verb, int mode, Common::TextToSpeechManager::Acti
 #endif
 
 		tmp = _charset->_center;
+		// 繁中化：指令列改用 chinese_verb.fnt。旗標只包住這一次 drawString，
+		// 句子列與對白仍走正文字型。
+		// 字模尺寸可能與正文不同（例如 24x24），連 _2byteWidth/_2byteHeight 一起換掉，
+		// 排版、advance、_charset->_str 邊界（進而 verb 的點擊範圍）就會自動跟著走。
+		//
+		// [重要] 句子列（「走到 查克盆栽」）也是走這個函式畫的，所以不能一律換。
+		// 實機 dump 出來的分工很清楚：
+		//     slot 27 / verbid 100 / top 145   ← 句子列
+		//     slot 3-11 / verbid 2-10 / top 152,168,184  ← 指令格
+		// 指令格的列距是 16 邏輯像素（32 實體），放得下 24x24；句子列到指令第一列
+		// 只有 7 邏輯像素（14 實體），一起放大就會疊在一起。所以只有指令格換字型，
+		// 句子列維持正文字型與尺寸。
+		const bool isSentenceLine = (vs->verbid == 100);
+		const int savedW = _2byteWidth, savedH = _2byteHeight;
+		if (_2byteVerbFontPtr && !isSentenceLine) {
+			_useVerbFont = true;
+			_2byteWidth = _2byteVerbWidth;
+			_2byteHeight = _2byteVerbHeight;
+		}
 		drawString(4, msg, ttsAction);
+		_useVerbFont = false;
+		_2byteWidth = savedW;
+		_2byteHeight = savedH;
 		_charset->_center = tmp;
 
 		if (isRtl)
